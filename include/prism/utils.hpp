#ifndef PRISM_UTILS_HPP
#define PRISM_UTILS_HPP

#include <string>
#include <vector>

void write_array(const std::vector<double> &arr, const std::string &filename = "c.txt");

#endif // PRISM_UTILS_HPP
